"use client";

import type { Dispatch, SetStateAction } from "react";
import type { Build, Mutation, Rank } from "../types/build";
import type { Monster } from "../types/monster";
import { CollapsibleSection } from "./collapsible-section";
import { Panel } from "./panel";

const mutations: { id: Mutation; label: string }[] = [{ id: "huge", label: "Huge" }, { id: "shiny", label: "Shiny" }, { id: "bloodlit", label: "Bloodlit" }, { id: "fairy", label: "Fairy" }];
const ranks: Rank[] = ["SS", "S", "A", "B", "C", "D", "E"];
const enhancements = Array.from({ length: 11 }, (_, index) => index);

function SelectField({ label, options, value, onChange, emptyLabel = "Select" }: { label: string; options: { id: string; label: string }[]; value: string | null; onChange: (value: string | null) => void; emptyLabel?: string }) {
  return <label className="block"><span className="mb-1.5 block text-[10px] font-bold uppercase tracking-[0.12em] text-[#788295]">{label}</span><select value={value ?? ""} onChange={(event) => onChange(event.target.value || null)} className="w-full rounded-md border border-[#303848] bg-[#171b25] px-3 py-2 text-sm text-[#d8dee9] outline-none focus:border-[#79e3ae]"><option value="">{emptyLabel}</option>{options.map((option) => <option key={option.id} value={option.id}>{option.label}</option>)}</select></label>;
}

export function BuildEditor({ monster, build, onBuildChange, onReset }: { monster: Monster | null; build: Build; onBuildChange: Dispatch<SetStateAction<Build>>; onReset: () => void }) {
  const update = <K extends keyof Build>(key: K, value: Build[K]) => onBuildChange((current) => ({ ...current, [key]: value }));
  const toggleMutation = (mutation: Mutation) => update("mutations", build.mutations.includes(mutation) ? build.mutations.filter((value) => value !== mutation) : [...build.mutations, mutation]);
  const updateAccount = (key: keyof Build["accountMultipliers"], value: boolean) => onBuildChange((current) => ({ ...current, accountMultipliers: { ...current.accountMultipliers, [key]: value } }));
  const selectedSkill = monster?.skills.find((skill) => skill.id === build.selectedSkillId);
  const updateLevel = (value: string) => { const level = Number(value); if (Number.isInteger(level) && level >= 1 && level <= 100) update("level", level); };

  return <Panel eyebrow="Step 2" title="Build Editor" action={<button type="button" onClick={onReset} className="text-xs font-medium text-[#99a2b3]">Reset</button>}><div className="flex flex-1 flex-col gap-3 overflow-auto p-5">{!monster && <div className="rounded-lg border border-dashed border-[#303848] bg-[#0d1017]/45 p-4 text-center"><p className="text-sm font-medium text-[#d8dee9]">No monster selected</p><p className="mt-1 text-xs leading-5 text-[#788295]">Pick a monster from the browser to begin editing its build.</p></div>}<CollapsibleSection title="Pet"><div className="grid grid-cols-2 gap-3"><label><span className="mb-1.5 block text-[10px] font-bold uppercase tracking-[0.12em] text-[#788295]">Level</span><input type="number" min="1" max="100" step="1" value={build.level} onChange={(event) => updateLevel(event.target.value)} className="w-full rounded-md border border-[#303848] bg-[#171b25] px-3 py-2 text-sm text-[#d8dee9]" /></label><SelectField label="Rank" value={build.rank} onChange={(value) => update("rank", value as Rank | null)} options={ranks.map((rank) => ({ id: rank, label: rank }))} /><SelectField label="Enhancement" value={String(build.enhancement)} onChange={(value) => update("enhancement", Number(value ?? 0))} options={enhancements.map((value) => ({ id: String(value), label: `+${value}` }))} /><label><span className="mb-1.5 block text-[10px] font-bold uppercase tracking-[0.12em] text-[#788295]">Genetic Potential</span><input type="number" min="0" max="100" value={build.geneticPotential} onChange={(event) => update("geneticPotential", Number(event.target.value))} className="w-full rounded-md border border-[#303848] bg-[#171b25] px-3 py-2 text-sm text-[#d8dee9]" /></label></div><label className="mt-3 block"><span className="mb-1.5 block text-[10px] font-bold uppercase tracking-[0.12em] text-[#788295]">Evolution Multiplier</span><input type="number" min="0" step="0.01" value={build.evolutionMultiplier} onChange={(event) => update("evolutionMultiplier", Number(event.target.value))} className="w-full rounded-md border border-[#303848] bg-[#171b25] px-3 py-2 text-sm text-[#d8dee9]" /></label></CollapsibleSection><CollapsibleSection title="Mutations"><div className="grid grid-cols-2 gap-2">{mutations.map((mutation) => <button key={mutation.id} type="button" onClick={() => toggleMutation(mutation.id)} aria-pressed={build.mutations.includes(mutation.id)} className={`rounded-md border px-3 py-2 text-sm ${build.mutations.includes(mutation.id) ? "border-[#79e3ae] bg-[#173126] text-white" : "border-[#303848] bg-[#171b25] text-[#99a2b3]"}`}>{mutation.label}</button>)}</div></CollapsibleSection><CollapsibleSection title="Skill"><SelectField label="Skill" value={build.selectedSkillId} onChange={(value) => update("selectedSkillId", value)} options={monster?.skills.map((skill) => ({ id: skill.id, label: skill.name })) ?? []} /><label className="mt-3 block"><span className="mb-1.5 block text-[10px] font-bold uppercase tracking-[0.12em] text-[#788295]">Multiplier Preview</span><input readOnly value={selectedSkill ? "Placeholder" : "Select a skill"} className="w-full rounded-md border border-[#303848] bg-[#171b25] px-3 py-2 text-sm text-[#788295]" /></label></CollapsibleSection><CollapsibleSection title="Equipment"><div className="grid grid-cols-2 gap-3"><SelectField label="Paw" value={build.pawId} onChange={(value) => update("pawId", value)} options={[{ id: "paw-1", label: "Paw 1" }, { id: "paw-2", label: "Paw 2" }]} emptyLabel="None" /><SelectField label="Ring" value={build.ringId} onChange={(value) => update("ringId", value)} options={[{ id: "ring-1", label: "Ring 1" }, { id: "ring-2", label: "Ring 2" }]} emptyLabel="None" /></div><div className="mt-3 rounded-md border border-dashed border-[#303848] p-3 text-xs text-[#788295]">Gear Attributes placeholder</div></CollapsibleSection><CollapsibleSection title="Account Multipliers" description="Settings saved across all pets"><div className="space-y-2">{[{ id: "indexMania", label: "Index Mania" }, { id: "petQuestAchievement", label: "Pet Quest Achievement" }, { id: "pathOfProgress", label: "Path of Progress" }].map((account) => <label key={account.id} className="flex items-center justify-between rounded-md bg-[#171b25] px-3 py-2 text-sm text-[#d8dee9]"><span>{account.label}</span><input type="checkbox" checked={build.accountMultipliers[account.id as keyof Build["accountMultipliers"]]} onChange={(event) => updateAccount(account.id as keyof Build["accountMultipliers"], event.target.checked)} className="accent-[#79e3ae]" /></label>)}</div></CollapsibleSection><div className="mt-auto grid grid-cols-2 gap-2 pt-2">{["Save Build", "Load Build", "Compare Builds"].map((item, index) => <button key={item} type="button" className={`rounded-md px-3 py-2.5 text-sm font-semibold ${index === 0 ? "bg-[#79e3ae] font-bold text-[#0b1510]" : "border border-[#303848] bg-[#171b25] text-[#d8dee9]"}`}>{item}</button>)}<button type="button" onClick={onReset} className="rounded-md border border-[#303848] bg-[#171b25] px-3 py-2.5 text-sm font-semibold text-[#d8dee9]">Reset</button></div></div></Panel>;
}
